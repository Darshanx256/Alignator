[package]
name = "alignator-formatters"
version = "0.1.0"
edition = "2021"
authors = ["DeepMind Team", "Darshan"]
description = "Formatters for lyric alignment output"

[dependencies]
alignator = { path = "../../core/rust" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
